# 2026-09-19

## ch03 融资交易与 Term Sheet：加注释 + 法务对话体
- 起因：Andy 反馈 ch03 看不懂，要求加注释和可折叠扩展，且要用「老板/投资人说大白话 → 法务用大白话答 → 翻译成条款」的对话形式。
- 产出（均在 `ch03-deal-terms.html`）：
  - 新增组件 `details.dlg`（法务对话体），8 组对话：01 估值与期权池 / 02 清算优先权 / 03 反稀释 / 04 回购对赌 / 05 否决权 / 06 TS 约束力与排他期 / 07 上市前清理 / 08 领售权与随售权。角色：郭明（老板·amber）、投资人（blue）、法务（green）。
  - 新增「零、先把黑话翻成人话」：19 词对照表（黑话／人话／为什么要在乎）。
  - 行内注释 `.anno` 13 处（虚线下划线 + 上标「注」，悬停看 title）。
  - 每个 h3 下加 `.plain` 一句话白话结论（5 处）。
  - 自检清单新增 2 条（公司回购的减资/担保决议；能否用大白话讲清清算优先与回购）。
- 样式：`assets/style.css` 末尾追加 `.dlg / .dlg-line / .dlg-note / .plain / .anno`，全部走 CSS 变量，深色模式自动适配。app.js **未改**——`data-fold-kind="dlg"` 已被通用折叠逻辑支持。
- 新建项目约束文件：`AGENTS.md`（五条硬规则 + 固定章节结构 + 写作风格 + 技术约束）、`MEMORY.md`（项目约定与待办）。原空间两者都缺，已按 SOUL.md / USER.md / README.md 推定补齐。

## 复核要点（法条口径）
- 回购对话中引用：《九民纪要》第 5 条；减资《公司法》2023 修订第 224 条；对外担保决议第 15 条；违约金酌减《民法典》第 585 条。已在正文标注「条号请以现行官方文本复核」。
- 五年利息：单利 40%、复利 46.9%（8%）。

## 踩坑：改了页面但「看不到」
- 现象：源文件已改（93KB），Andy 反馈看不到。原因**不是缓存**，是这个站有三层：源站 `融资法务/` → 部署副本 `linqiongni_profile/public/financing-legal/` → 线上 `linqiongni.top`（GitHub Actions push main 才构建发布）。漏了后两步。
- 已处理：改了 `linqiongni_profile/package.json` 的 `sync:financing`，加上 `--exclude AGENTS.md --exclude MEMORY.md --exclude .workbuddy`（避免把内部工作文件发布到公网），并执行了一次同步，public 副本已更新到 93KB。
- 上线仍需 `git commit + push main`（`.github/workflows/deploy.yml`：push main → npm ci → vite build → Pages）。push 属对外发布，已询问 Andy 后再动。

## 已 push 上线（commit 421b76e）
- Andy 回复「都push」→ 连同仓库里遗留的 labor / 婚姻家事 / 法律AI / 视频号文档 一并提交。
- **拦下一个事故**：`视频号策划与运营/06-资产素材/` 里有 148MB mp4 + 65MB mov，GitHub 单文件硬上限 100MB，push 会被拒。已 `git rm --cached` 移出，并在 `.gitignore` 加了 `*.mp4 / *.mov / 视频号策划与运营/06-资产素材/`。**以后「全部提交」前必须先看暂存区有没有大文件。**
- 另一个常踩的坑：这个仓库里 git 写操作会残留 `.git/index.lock` / `refs/remotes/origin/main.lock`，导致后续命令报 "Running another git process"。解法：`rm -f .git/index.lock .git/refs/remotes/origin/main.lock`，且 git 命令要在解除沙箱下跑。
- 验证：远端 main = 421b76e；`https://linqiongni.top/financing-legal/ch03-deal-terms.html` 返回 200，92941 字节，含 9 个 dlg 块 —— 线上已是新版。

## 第二轮：对话还是看不懂 → 加「就好比」生活比方（commit 8cc545a）
- Andy 反馈：对话里「TS 第 3 条要求交割前把期权池扩大到 10%」这种句子仍然看不懂，要求**法律句子后面紧跟一句「就好比xxx」**，用日常生活场景打比方。
- 新增组件 `.sim`（金底左边框小块，`<b>就好比</b>` 开头），插在 `.utt` 段落末尾，共 **35 处**，覆盖 8 组对话里所有法律腔句子。
- 比方素材（可复用到其他章节）：卖房划面积、合伙摆摊散伙、电影票降价补票、车险「全损」、欠条签本人还是签店、利滚利、夫妻共有房过户、小区卖楼、婚介独家期、挂牌前拆挂钩、团购凑满减、班级平均分。
- **写法规则（重要，后续章节沿用）**：先说法律结论 → 立刻「就好比」日常场景 → 再（在 .dlg-note 里）翻成条款。三段顺序不能乱，比方必须紧贴那句难懂的话，不能攒到段末。
- 页首「怎么读这一页」已说明这条读法；hero meta 标注「8 组法务对话 + 35 处生活比方」。
- 线上已验证：200，101773 字节，35 处 sim。已是新版。

## 第三轮：线上已更新但仍「看不到」→ 第 4 层排查（缓存 + 折叠，commit 3f0e56d / 03754db）
- 现象：三层（源站/部署副本/线上）都已更新，curl 线上能 grep 到 35 处 sim，Andy 仍说看不到。
- **原因一：资源没版本号**。融资法务站 14 个页面全裸引 `assets/style.css`，浏览器命中旧 CSS → 新增 `.sim` 样式不存在，视觉上等于没改。已全站加 `?v=20260919b`（婚姻家事站此前踩过同一个坑，用同样做法）。
- **原因二：iframe src 写死**。主页 tab 用 `<ThemeIframe src="/financing-legal/index.html">`，主站重建后 iframe 仍拉旧内容。已改成挂日期版本号 `?v=${IFRAME_V}`（`new Date().toISOString().slice(0,10)`），以后自动生效不用手改，其余 iframe tab 如需照搬。
- **原因三（最致命）：9 个对话块全部 `<details class="dlg">` 默认收起**。Andy 看到的是「页面和以前一样」——新内容全折着。已把黑话对照表 + 对话 01 改成 `open` 作示范，其余保持收起 + 顶部有「展开全部对话」按钮。
- 教训：**新增内容放进折叠块时，务必让第一块默认展开**，否则用户根本不知道里面多了东西。GitHub Pages 的 `cache-control: max-age=600` 只能兜 10 分钟，版本号才是根治。
- 已写入技能 `profile-site-sync-publish`（新增「缓存与折叠（第 4 层）」一节）。

## ch04 尽调站：套用 ch03 同套组件（commit 65ba312）
- Andy 要求「按同样方式改 ch04」。ch04 原本没有对话体，按 ch03 已验证的三段式补齐，全套组件已可复制：
  - 「零、先把黑话翻成人话」14 词对照表（黑话／人话／老板为什么要在乎），**默认展开**
  - 8 组 `details.dlg` 对话：01 217 项清单与三角验证 / 02 陈述与保证 vs 承诺事项 / 03 披露函 / 04 赔偿 vs 违约责任（期限·门槛·上限三件套）/ 05 社保敞口价格化与四出口 / 06 红黄绿灯与摘要页 / 07 关联方与承租主体（对应案例 04）/ 08 四出口矩阵与退出权补救期
  - 24 处 `.sim` 比方，素材主线是**体检**（ch03 主线是买房/相亲）：化验单三色、体检前自查、主动说 vs 被发现；另有卖二手车（R&W vs covenant）、卖房状况说明书（披露函）、装修保修三件套（赔偿）、卖连锁店铺位在老板娘名下（关联方）
  - `.anno` 8 处 + 6 个小节 `.plain` 一句话结论 + 页首「怎么读这一页」+ 展开/收起全部对话按钮
- 文件 186 行 → 51514 字节；结构校验通过（details 9/9，无未闭合）。
- **ch04 没有模板库**，所以 `.dlg-note` 里不引模板编号，改为指向本页章节／清单条目（如「落点：本节案例 04」）——后续改造无模板的章节沿用这个写法。
- 法条引用已标注复核：社会保险法 86 条（滞纳金按日万分之五、罚款 1–3 倍）、劳动合同法 82 条（二倍工资）、民法典 584 条（可预见性）。

# 2026-09-19

## ch03 融资交易与 Term Sheet：加注释 + 法务对话体
- 起因：Andy 反馈 ch03 看不懂，要求加注释和可折叠扩展，且要用「老板/投资人说大白话 → 法务用大白话答 → 翻译成条款」的对话形式。
- 产出（均在 `ch03-deal-terms.html`）：
  - 新增组件 `details.dlg`（法务对话体），8 组对话：01 估值与期权池 / 02 清算优先权 / 03 反稀释 / 04 回购对赌 / 05 否决权 / 06 TS 约束力与排他期 / 07 上市前清理 / 08 领售权与随售权。角色：郭明（老板·amber）、投资人（blue）、法务（green）。
  - 新增「零、先把黑话翻成人话」：19 词对照表（黑话／人话／为什么要在乎）。
  - 行内注释 `.anno` 13 处（虚线下划线 + 上标「注」，悬停看 title）。
  - 每个 h3 下加 `.plain` 一句话白话结论（5 处）。
  - 自检清单新增 2 条（公司回购的减资/担保决议；能否用大白话讲清清算优先与回购）。
- 样式：`assets/style.css` 末尾追加 `.dlg / .dlg-line / .dlg-note / .plain / .anno`，全部走 CSS 变量，深色模式自动适配。app.js **未改**——`data-fold-kind="dlg"` 已被通用折叠逻辑支持。
- 新建项目约束文件：`AGENTS.md`（五条硬规则 + 固定章节结构 + 写作风格 + 技术约束）、`MEMORY.md`（项目约定与待办）。原空间两者都缺，已按 SOUL.md / USER.md / README.md 推定补齐。

## 复核要点（法条口径）
- 回购对话中引用：《九民纪要》第 5 条；减资《公司法》2023 修订第 224 条；对外担保决议第 15 条；违约金酌减《民法典》第 585 条。已在正文标注「条号请以现行官方文本复核」。
- 五年利息：单利 40%、复利 46.9%（8%）。

## 踩坑：改了页面但「看不到」
- 现象：源文件已改（93KB），Andy 反馈看不到。原因**不是缓存**，是这个站有三层：源站 `融资法务/` → 部署副本 `linqiongni_profile/public/financing-legal/` → 线上 `linqiongni.top`（GitHub Actions push main 才构建发布）。漏了后两步。
- 已处理：改了 `linqiongni_profile/package.json` 的 `sync:financing`，加上 `--exclude AGENTS.md --exclude MEMORY.md --exclude .workbuddy`（避免把内部工作文件发布到公网），并执行了一次同步，public 副本已更新到 93KB。
- 上线仍需 `git commit + push main`（`.github/workflows/deploy.yml`：push main → npm ci → vite build → Pages）。push 属对外发布，已询问 Andy 后再动。

## 已 push 上线（commit 421b76e）
- Andy 回复「都push」→ 连同仓库里遗留的 labor / 婚姻家事 / 法律AI / 视频号文档 一并提交。
- **拦下一个事故**：`视频号策划与运营/06-资产素材/` 里有 148MB mp4 + 65MB mov，GitHub 单文件硬上限 100MB，push 会被拒。已 `git rm --cached` 移出，并在 `.gitignore` 加了 `*.mp4 / *.mov / 视频号策划与运营/06-资产素材/`。**以后「全部提交」前必须先看暂存区有没有大文件。**
- 另一个常踩的坑：这个仓库里 git 写操作会残留 `.git/index.lock` / `refs/remotes/origin/main.lock`，导致后续命令报 "Running another git process"。解法：`rm -f .git/index.lock .git/refs/remotes/origin/main.lock`，且 git 命令要在解除沙箱下跑。
- 验证：远端 main = 421b76e；`https://linqiongni.top/financing-legal/ch03-deal-terms.html` 返回 200，92941 字节，含 9 个 dlg 块 —— 线上已是新版。

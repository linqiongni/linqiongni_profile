# 2026-09-19

## 商事仲裁站：改为左侧固定目录栏布局
- 需求：Andy 要求商事仲裁手册做成「像法务实务-保险·法律维权那样在左边固定的目录栏」。
- 结论：保留 20 个独立 HTML 页（直链不失效、单页 15–26 KB 加载快，不像保险站 1.1MB data.js 一次加载），只改布局层：顶栏（品牌＋章节搜索＋当前章 chip＋窄屏「目录」按钮）＋左侧 280px 固定目录栏（4 个分组：总纲 / PART I / II / III，当前章高亮并自动滚入可视区，底部法源基准小卡）＋主区独立滚动＋右侧 208px 本页目录（h2/h3 自动生成，滚动高亮，≥1181px 显示；窄屏回落为页内目录块）。
- 改动文件：`商事仲裁/assets/style.css`（整体重写版式层）、`商事仲裁/assets/app.js`（DOM 重构 + 侧栏构建 + 搜索 + scrollspy + 抽屉），已 rsync 到 `public/arbitration/`。
- 搜索加了章节 keywords（kw 字段），否则「撤裁」这类简称搜不到「申请撤销仲裁裁决」。
- 校验：jsdom 冒烟（index/ch10/ch18）——侧栏 21 链接、高亮 1、右侧目录条目与页内 h2/h3 数一致、搜索命中 1。commit `4627f31` 已 push。

## 婚姻家事与遗产继承律师实务手册上线（新「律师实务」顶层分组）
- 新增仓库根目录源站 `婚姻家事与遗产继承/`：index 总纲 + ch01–ch24 共 25 页（约 560KB），B 布局复用商事仲裁站 style/app.js 模式。
- 结构：PART I 通用全流程 ch01–08（接待/诉前/保全/取证/调解/庭审/生效执行/与商事差异）；PART II 十案由 ch09–18（每章统一五节：焦点/文书起草/证据清单/法条引用/抗辩坑点）；PART III 特殊程序 ch19–23（保护令/特别程序/执行难点/遗产管理人/再审+三撤+执行异议）；ch24 附录（法条索引/期限速查/文书清单）。
- **法源基准**（联网核实，事实表在 `婚姻家事与遗产继承/_build/法源基准.md`，含三份解释全文）：婚姻家庭编解释（二）＝法释〔2025〕1 号 23 条 2025-02-01 施行（全文逐条已存）；继承编解释（一）＝法释〔2020〕23 号 45 条全文已存；**网传「2026 继承新规」不存在**；民法典 1158 条=遗赠扶养协议（spp.gov.cn 核实）；反家暴法 23/27/28/29/30/34 条与法释〔2022〕17 号 1/2/3/4/6/10/12 条已核；民诉法 2023 修正后一律写「相关规定」不写条号。
- 挂载：**新顶层分组 `practice`（律师实务）** subTabs:['family-law']；types.ts 加 'family-law'；navConfig 分组+SUB_TAB_META；App.tsx 四处接线；组件 `FamilyInheritanceTab.tsx`（ThemeIframe → `/family-law/index.html`）；package.json 加 `sync:family-law`（rsync 排除 _build）。
- commit a1e4360（手册+tab）+ dd2ec92（Hero 数据条 11 领域/470+ 篇）已 push；线上验证 bundle 含 `id:"practice",subTabs:["family-law"]`，`/family-law/` 200。
- 踩坑复现：App.tsx 多行条件 Edit 两次报成功未落盘（只有 import/渲染分支成功），python replace 才写入——**改后必 grep 计数**。
- 写作管线：5 个并行子代理分章撰写，统一先读 `_build/法源基准.md` + 商事仲裁页模板，产出后独立跑标签配对自检（25 页全平衡）。

## 新建「身边的英语」口语学习站（独立单文件，不进 profile 构建）
- 位置：`身边的英语/index.html`（纯静态单文件，HTML+CSS+JS 全内嵌，双击即开，无外部依赖）。
- 定位：教中文母语者地道美式口语，主题为「广州上班族完整的一天」。工作日 13 模块（起床→睡前）+ 休息日 5 模块占位（待 Andy 确认风格后补）。
- 技术：Web Speech API 发音（en-US/en-GB 切换、正常/慢速 0.75x、onvoiceschanged 兜底加载）；工作日/休息日大 Tab；模块 chip 导航；搜索（英/中/替换说法）；localStorage 收藏「已会」+ 进度条；回到顶部；移动端优先、鼠尾草绿配色（避紫）。
- 内容铁律：缩读连读（gonna/wanna/gotta/lemme/gimme/kinda）、迷你对话标注 Me/Partner/Coworker、每句 1–2 同义替换；广州通勤用 scan the QR code to get through / the last mile on a shared bike / jam-packed 等地道说法。
- 校验：node `new Function` 语法通过。下一步待 Andy 确认地道风格后再写休息日 5 模块（Sleeping In / Brunch / Chores / Going Out / Sunday Evening）。

## 身边的英语 → linqiongni.top 上线 + 首页 ENGLISH tab
- 吸顶修复：原 `.chips` 导览行 `position:sticky;top:0` 与 header 同为 top:0 会被 header 盖住消失；改为把 chips 移入 sticky `header.top` 内（header 单一吸顶，chips 随之常驻），`.chips` CSS 去掉 sticky/top/z-index/background。改的是 `身边的英语/index.html`。
- 上线接线（复用 family-law 的 ThemeIframe 模式）：`src/types.ts` TabType 加 'english'；`src/navConfig.ts` 新增顶层分组 `id:'english',label:'ENGLISH'`（subTabs:['english']）+ SUB_TAB_META 条目；`src/App.tsx` 三处接线（import + 渲染分支 + isFullBleed/scrollTop 列表加 'english'）；新增 `src/components/EnglishTab.tsx`（ThemeIframe src="/english/index.html"）。
- 资源：`身边的英语/` 为源站，`npm run sync:english` rsync 到 `public/english/`（已加 sync 脚本 + 并入 sync:all-static）；`public/english/index.html` 单文件全内嵌、无外部依赖。
- 本地 `npx tsc --noEmit` 通过；js 语法复核通过。
- **阻塞：git push origin main 失败**（沙箱连 github.com:443 超时，AGENTS.md 已记录的偶发问题）。commit 已本地完成（7 files, +1557），待用户自推或网络恢复后重试，CI 会自动构建部署到 linqiongni.top/english/。
- 12:32 用户要求 push：retry 返回 `Everything up-to-date`，确认 commit e38444f 上次其实已成功上 origin/main（仅未收到返回）。CI 已构建部署，线上 `/english/` 与首页 ENGLISH 分组应已生效。

## 身边的英语：左侧固定目录栏 + 可折叠（截图反馈）
- 需求（截图）：目录栏 + 搜索栏固定到左手边，可左折叠收起。
- 实现：单文件内新增 `.sidebar`（fixed 左、宽 280px，含 brand/搜索/口音·语速切换/工作日·休息日 Tab/场景目录 chips 竖排）+ `.main`（margin-left:280px）+ `.backdrop` 遮罩。`.app.sidebar-closed` 控制收起（translateX(-100%)）。
- 折叠交互：桌面端侧栏内「◀」collapse-btn 收起、移动端顶栏「☰」menu-btn 打开；窄屏(<1024px)侧栏变抽屉，打开时 body 锁滚 + 遮罩点击关闭；点击目录项滚动后窄屏自动收起。滚动时 scrollspy 高亮当前模块 chip（updateChipActive）。
- 用 Python 脚本改写避免漏改，临时脚本已删除；`npm run sync:english` 同步到 public/english/。
- 校验：node new Function 语法通过。commit 4a0f56e 已 push（ec5d47b..4a0f56e），CI 部署到 linqiongni.top/english/。
- 14:53 修复：桌面端折叠后无重开入口（顶栏 ☰ 在桌面被隐藏、侧栏 ◀ 收起后也隐藏）。新增 `.open-btn` 悬浮「☰ 目录」按钮，仅 `.app.sidebar-closed` 且 ≥1024px 时显示，点击 openSidebar；移动端仍用顶栏 menu-btn。commit b577875 已 push。

## 刑事辩护 tab：移入「律师实务」+ 改左栏目录
- 需求：Andy 要求把「法务实务-刑事辩护」移到「律师实务」分组，并做成婚姻家事同款左侧目录。
- 做法：navConfig 把 `criminal` 从 `legal.subTabs` 移到 `practice.subTabs`（现 `[family-law, criminal]`）；`CriminalDefenseTab.tsx` 入口改 `/criminal/index.html`；`App.tsx` 的 `isFullBleed` 补 `|| activeTab === 'criminal'`（通栏，否则 iframe 被 max-w 约束）。
- 左栏 B 布局改造：合并 13 页内联 `<style>` 去重 → 共享 `public/criminal/assets/crim-style.css`（暗色国风 + 家事站布局层）；写 `assets/crim-app.js`（`STATIONS` 驱动 13 页侧栏，分组 总纲/PART I 全流程(8)/PART II 工具与模板(3)/PART III 训练计划(1)，含搜索/scrollspy/上下篇/复制兜底）；13 页改写：去内联 style、去旧顶栏 nav、加 `<body data-ch>`、删外链 theme-toggle 后挂 app.js+theme-toggle。**必须保留各页内联 `<script>`**（plan.html 的 30 天 DAYS 渲染、templates/tools 内联脚本），只删外链 script。
- 校验：tsc OK；jsdom 冒烟（index/plan/templates/tools）侧栏均 13 链接、当前页高亮、右目录条目数=h2/h3 数、搜索「会见」命中 3、plan 内联脚本渲染成功；13 页标签配对全平衡。commit a54f5a7 已 push；线上 bundle 确认 `practice.subTabs:["family-law","criminal"]`，`/criminal/` 及各资源均 200。

## 2026-09-19（午间）· 移除首页 Hero 底部内容地图导航
- 需求：Andy 截图反馈首页下方「个人 / 法务实务 / 律师实务 / 创业奇思妙想章 / 影视法律」展开的小字导航显得杂乱，要求移除。
- 根因：该区块在 `src/components/Hero.tsx` 中称为「内容地图」，贴左下角，用 `NAV_GROUPS` + `SUB_TAB_META` 渲染各分组下的 subTab 链接。
- 改动：删除 Hero.tsx 第 90–131 行内容地图块；移除未使用的 `ChevronDown`、`NAV_GROUPS`、`SUB_TAB_META`、`TabType` 导入；`HeroProps` 清空，`Hero` 改为无 props 纯展示组件。同步在 `App.tsx` 移除 `handleScrollToContent` 函数定义与传给 Hero 的 `onScrollToContent`/`onExploreTab` 属性（保留 `contentSectionRef`，因仍被 tab 滚动逻辑使用）。
- 校验：`npx tsc --noEmit` 通过；commit `33dea71` 已 push 成功。

## 商事仲裁 tab：移入「律师实务」分组
- 需求（Andy 说「继续」，无新指令，按最合理默认）：商事仲裁本就是左栏目录 B 布局的律师办案手册（20 章仲裁全流程），与 family-law/criminal 最一致，故把它也并入「律师实务」分组。
- 改动：navConfig 把 `arbitration` 从 `legal.subTabs` 移到 `practice.subTabs`（现 `[family-law, criminal, arbitration]`）。`ArbitrationTab.tsx`（ThemeIframe → `/arbitration/index.html`）与 `App.tsx` 渲染分支/`isFullBleed` 已含 arbitration，无需改。
- **踩坑复现**：navConfig 两次 Edit 中只有 practice 那次落盘，legal 行仍含 arbitration，导致首版 commit 后线上 legal 与 practice **同时**含 arbitration；commit 显示「1 file changed, 1 insertion(+), 1 deletion(-)」已暴露只改了 1 行。改用 python replace 移除 legal 行残留，二次提交 `ec5d47b`。结论：**多行 Edit 改同一文件后必 grep 计数 + 看 commit diff 行数**，或直接用 python 改。
- 线上复验：bundle `practice.subTabs:["family-law","criminal","arbitration"]`，`legal` 已无 arbitration，`/arbitration/` 200。

## 保险·法律维权 tab：移入「律师实务」分组
- 需求（Andy 指定）：把「法务实务-保险·法律维权」也并入「律师实务」。
- 改动：navConfig 把 `insurance` 从 `legal.subTabs` 移到 `practice.subTabs`（现 `[family-law, criminal, arbitration, insurance]`）。保险静态站 `/insurance/` 本就是左栏目录 B 布局（app.js 驱动 + data.js 1.1MB 一次加载，正是商事仲裁改左栏时的参考对象），`InsuranceTab.tsx`（ThemeIframe → `/insurance/index.html`）与 `App.tsx` 渲染分支/`isFullBleed` 均早已含 insurance，无需改。
- **本次改用 python 改 navConfig**（避 Edit 不落盘坑），commit 正确显示「2 insertions, 2 deletions」即两行都改；push `a82c2a8`。线上复验 `practice.subTabs` 含 insurance、`legal` 已无、`/insurance/` 200。

## 左固定目录栏统一加「左折叠收起」
- 需求（Andy）：融资法务的目录页在左边固定且能折叠，其他左固定目录栏也要能折叠。
- **核实结论**：融资法务（/financing-legal/）实际是**顶栏导航**（`#topbar` + `.wrap`，无 `#side`/`aside`），并没有左固定目录栏——用户记忆偏差。真正左固定目录栏的站是：**商事仲裁 /insurance /ai-law（法律AI）**。
- 统一做法（三站一致）：侧栏顶部加 `.side-hd`（目录标题＋`⟨`折叠按钮）；折叠后 `#side`/`.sidebar` flex-basis→0 收起，左侧浮出 `.side-fab`（`⟩ 目录` 竖贴边）点击展开；状态写 `localStorage`（arb_/ins_/ailaw_side_collapsed）跨页记忆；折叠 CSS 只在 desktop 媒体查询内生效，不干扰窄屏抽屉（menu-btn）。
- 改动：商事仲裁 `assets/app.js`+`style.css`（源站，已 rsync 到 public/arbitration）；`public/insurance/app.js`+`styles.css`；`public/ai-law/index.html`（内联 CSS＋HTML 头＋JS，无源同步）。
- 校验：三站 node --check 通过；jsdom 冒烟（collapse click→body.side-collapsed=true，fab click→false）全通过；CSS 括号平衡；线上三站 grep `side-collapsed` 命中。commit `45e4d4d` 已 push。
- **复用的 B 布局左栏站清单**（同款 `.side`/scrollspy/搜索/折叠）：arbitration、insurance、ai-law、family-law、criminal、labor、ip-law、logistics-law、foreign-contracts（后者部分是页内 side 块）。新增左栏站直接复用该模式。

## 融资法务：顶栏导航 → 左侧固定目录栏（带折叠）
- 需求（Andy 确认「做」）：把融资法务从顶栏导航改成左固定目录栏（带折叠），与商事仲裁/保险/法律AI 统一视觉。
- **关键**：融资法务源目录是 `融资法务/`（rsync 到 `public/financing-legal/`，排除 README.md），必须改源目录而非 public，否则 sync 被覆盖。改动文件：`融资法务/assets/app.js`（327 行，重写）+ `融资法务/assets/style.css`（末尾追加布局块，内容样式全保留）。
- 实现：app.js 在 DOMContentLoaded 前用 IIFE 立即重构 DOM —— 把 `.wrap`+`footer.site` 重挂进新 `.layout`（顶栏 sticky + `#side` 左栏 + `.main` 主区 + `#pagetoc` 右栏）；STATIONS 14 项分 4 组（总纲 / PART I 创业与交易设计 01–05 / PART II 架构·合规·治理 06–09 / PART III 港股上市 10–13）。**保留**融资原有主题同步（postMessage+localStorage+切换按钮）、`data-deep` 知识锚点、`data-fold` 折叠控件。无需逐页改 14 个 HTML（旧空 `#topbar` 占位由 JS 物理移除，避免重复 id）。
- 折叠状态 key：`fin_side_collapsed`；`fitHeight()` 实测顶栏高度后注入 `layout`/`.side` 高度，宽屏主区滚动、窄屏侧栏变抽屉（menu-btn），CSS `side-collapsed` 仅 desktop 生效。
- 校验：jsdom 冒烟（去掉 url 选项，否则会解析到线上旧 app.js）全过——layout/side/main/pagetoc=true、`#topbar` 计数=1、4 组 14 链接、当前高亮、右栏 scrollspy、折叠切换、搜索「港股」命中 4 篇、主题切换、pager/footer 正常。commit 待 push。
- **jsdom 踩坑**：`JSDOM.fromFile` 若带 `url: https://...` 选项，相对 `assets/app.js` 会解析到线上旧版而非本地文件，导致测试跑旧代码——本地测试务必不带 url，或改 file://。

# 2026-09-12

## 餐饮法务 tab：每周计划交互改造
- 需求：点击「第 x 周第 x 天」标题即跳转链接，不要右侧「站内阅读」徽标 + 分享小图标。
- 改动 `src/components/CateringLegalTab.tsx`：
  - 每条 lesson 改为整行 `<a>`：有站内全文（`public/lessons/{id}.html`，免登录）→ 跳站内；否则跳资料库原文 `https://www.workbuddy.cn/space/d/{id}`。`target="_blank"`。
  - 删除右侧「站内阅读」徽标 + BookOpen 图标 + ExternalLink 分享图标；删除整块 inline 阅读面板（`activeLesson`/`readerRef`/`openLesson` 一并移除）。
  - hover 态保留：文字转金 + 下划线，作为可点提示。
  - 更新底部说明文字。
- 移除未用 import：BookOpen / ArrowUpRight / X / useRef。
- 校验：`npx tsc --noEmit` 通过。
- commit `2775187` 已 push 到 origin/main（提权放行），CI 会重建发布。
- 判断：用户要的是「直接跳」，所以统一开新窗口。若用户想「同标签跳转」，改 `target="_blank"`→去掉即可（待确认）。

## 补齐项目级 AGENTS.md / MEMORY.md
- 用户「约束先行」规则要求每会话开头建好，此前两回合漏建，本回合补齐于仓库根。
- 内容含：技术栈、部署红线（勿本地 build）、图片彩色偏好、波纹克制、餐饮法务跳转规则。

## 首页装饰猫咪（新增需求）
- 需求：第一屏（Hero）加可爱猫咪——右上角蜷成书页卷、左下角翻肚皮睡觉、右下角招手并说「hi，终于等到你～我是Andy」。
- 新增 `src/components/HomeCats.tsx`（三只手绘 SVG 猫，暖橙+奶白+棕描边），挂载进 `Hero.tsx`（`HomeCats` 置于几何圆点层之上、正文 z-10 之下，`pointer-events-none` 不拦交互）。
- 动画克制：蜷猫轻微呼吸+微旋、睡猫飘 Zzz+呼吸、招手猫摆爪+气泡淡入；`useReducedMotion` 时全静态。
- 气泡在 <sm 屏隐藏（避免小屏拥挤），仅猫本体显示。
- 校验：`npx tsc --noEmit` 通过；dev `:5177` 模块编译 200。
- commit `840da32` 已 push，CI 重建发布。
- 待确认：猫咪位置/大小/是否要更明显，旋钮都在 `HomeCats.tsx` 顶部定位 class 与 `Hero` 内 `<HomeCats />`。

## 删除「养成计划」tab（用户决策）
- 用户要求删掉「养成计划」tab 及其内容。已执行并 push（commit `c6204b1`，origin/main）：
  - 删 `src/components/DevelopmentPlanTab.tsx` 与 `public/development-plan.html`（tab 内嵌内容页）。
  - 移除 `App.tsx`（import + tab 切换项 + 渲染）、`Navbar.tsx`（TABS 数组项）、`types.ts`（`TabType` 去掉 `'plan'`）。
  - `npx tsc --noEmit` 通过，src 内零残留。
- 注意：上回合 commit/push 命令输出为空（显示 glitch），但实际已落地；用户复读指令时先 `git log`/`ls` 核实，确认已删再回复，勿重复删。
- 残留：CateringLegalTab 内「餐饮加盟法务总监养成计划」仅为其数据源名称（餐饮法务 tab 的数据来自该资料库），非被删 tab，保留不动。

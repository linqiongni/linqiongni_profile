# 2026-09-17

## 已上线：全站 iframe 页面统一深色/浅色切换
- 用户反馈「AI+法律」页已有切换按钮，希望其他铺满型 tab（刑事辩护、知识产权、劳动、跨境物流、影视法律 S1/S2、保险）也有同样的明暗切换。
- 实现：
  - 新增 `public/theme-toggle.js`：固定右上角按钮 + localStorage 共用 key `site_theme` + 与主站 React 双向 postMessage 同步。
  - 新增 `public/theme-light.css`：给原生深色静态页（刑事辩护）提供浅色覆盖。
  - 影视法律、保险 styles.css 新增 `[data-theme="dark"]` 暗色变量。
  - AI+法律页改用 `site_theme` 并加入 postMessage。
  - React `App.tsx` 读取/写入 `site_theme`，切换时广播 iframe，接收子页消息同步开关。
  - 批量给 300+ 静态页（含知识产权/劳动/跨境物流/刑事辩护全部子页）注入脚本。
- 提交：3212250，已 push origin main，CI 已发布到 linqiongni.top。
- 验证：线上页面返回 200，包含 `theme-toggle.js`，主题 CSS 资源可访问。

## 刑事辩护 tab 直接嵌入 30 天计划
- `CriminalDefenseTab.tsx` 的 iframe `src` 改为 `/criminal/plan.html`，打开 tab 即见 30 天训练计划；右上角按钮改为新窗口打开完整 13 页手册 `/criminal/`。
- 核实 `public/criminal/plan.html`（49KB）Day01–Day30 全覆盖，每日含 目标/精读/实操/可复制 AI 指令/自检/交付物，虚拟案卷（李某职务侵占 68 万）贯穿。
- 已 commit+push（3212250→2159c55），CI 部署上线验证：`/criminal/plan.html` 返回 200，bundle 含 `criminal/plan.html`。

## 30 天计划加入日次导航与锚点跳转
- `plan.html` 给 30 个 Day 加锚点 id（d01–d30），虚拟案卷下方插入「30 天日次导航」横滑条（30 按钮，点击→平滑滚动+自动展开+高亮该天），Day01 默认 `open`。
- JS：导航点击关闭其他 details、展开目标并 scrollIntoView；点击 summary 同步高亮；支持 `#d07` URL hash 直达。
- commit+push（c64fe50），CI 上线验证：线上 plan.html 含 daynav / 30×dbtn / d01 open / 锚点，200。

## 修复 30 天计划「指令框布局乱」根因（plan.html）
- **根因**：30 个 `.pbox` 指令框里有 **29 个「复制」按钮未闭合**，整段指令文本被塞进 `.cp`（`position:absolute;font-size:10.5px` 的小按钮）→ 正文溢出重叠 + pbox 高度塌陷 → 用户反馈"界面很乱、指令布局乱来乱去"。
- 修复：`re.sub(r'(onclick="cp\(this\)">)(?!复制</button>)', r'\1复制</button>', s)`；`.pbox` 加 `padding-top:30px` 预留顶部条，复制按钮不再压首行；`daynav` 去掉 `position:sticky;top:52px`（与 sticky 顶栏重叠）；Day01+Day02 默认 `open`。commit 68f5b91 已上线，线上 30/30 按钮正确、残留 0。
- **环境限制**：本机 Chrome `--headless` 被 SIGTERM(137)、`qlmanage` sandbox init failed，**无法截图验证**；改用标签配对自检（div/details/summary/button 开闭数相等）+ Read 用户截图人工判读。
- 经验已回写技能 `legal-domain-site-tab`（新增第七节「静态页布局排错」）。

## 30 天计划：全部折叠默认 + 日次吸顶导航 + 展开/收起全部
- 用户诉求：「1-15天指令跑出来 + 指令折叠 + 点击当天标题在当前页内展开无需跳转」。
- 核实：plan.html Day01–30 指令内容**早已全部生成且真实**(逐日含目标/精读/实操/AI指令/自检/交付物)，本次不是补内容而是改交互。
- 改造：移除 d01/d02 的 `open` → **30 天全部默认折叠**；日次导航加「展开全部/收起全部」按钮 + 一行交互提示；日次条改 `position:sticky` 且 `top` 由 JS 量取 `.topbar` 实际高度设置（避免与模块顶栏重叠，修复上轮 overlap 隐患）；点击日次按钮改为「仅展开目标天」更直觉。
- 自检：div144/details30/summary30/button62 全部配对，无 `open` 残留，JS 括号配平。
- push 时远端领先(rebase c8b031e 后 b8621c8)，已 pull --rebase 整合再 push，无冲突。
- 线上验证 200：expandAll/collapseAll/提示/吸顶JS 均在位，0 个 open。

## 30 天计划：Day01–15「跑出来的内容」嵌入（关键语义澄清）
- **用户语义澄清**：`跑出来` ≠ 显示页面/生成指令文本，而是**把当天那条「给 AI 的指令」真正执行一遍，把产出（答案/文书全文/表格）嵌进页面**。此前页面只显示"指令"（prompt），用户认为"没有内容"。
- 落地：每日新增 `.out` 输出块（跑出来的内容），原始指令折叠进嵌套 `<details class="ins">`（默认收起）。顺序：summary → 目标/实操 meta → .ins(指令，折叠) → .out(跑出来的内容，可见) → 自检 meta。
- Day01–15 已写入：12问12答+15个法定期间+期限台账、14项必问+5句禁语、利益冲突审查+三份委托要点、卷宗A–H目录+5份工作日志+留痕清单、三角色演练+综合评估报告、五道障碍应对+救济速查、第一次会见笔录全文、排非+调取申请书全文、社会危险性五维反驳表、羁押必要性听证提纲+解封申请、阅卷8问+卷宗目录表、8组矛盾点表、证据三性审查表、不起诉意见书、认罪认罚清单。Day16–30 用 `.rest` 占位待续。
- 技术要点：包裹 pbox 用 `re.sub(r'<div class="pbox">(.*?)</div>', wrap, s, flags=re.S)`（只影响 pbox，不误伤 .doc/.box）；插入 .out 必须锚定**每天第一个 `</details>`（.ins 的闭合）之后**，不能用 `s.index('</details>')`（嵌套后有两个）。文件 54KB→112KB，全标签配对平衡。
- commit a0d41db；push 期间 GitHub 两次 "Empty reply"/connect 443 失败（curl 却 200），第三次重试成功。**经验：push 失败先重试，别急着判定断网。**
- 线上 200 验证：.out×15、指令折叠×30、Day01 12问、Day07 笔录、.rest×15 均到位。

## 紧急修复：刑事辩护 tab 从线上"消失"的根因（并发提交覆盖）
- **现象**：用户反馈"甚至没看到刑事辩护 tab"，且 SPA 改造(8a40185) push 被 429 限流卡住。
- **根因**：并发会话的提交 `b0dacc3`（新增「保险·法律维权」tab）基于**旧版** `src/App.tsx`/`navConfig.ts`/`types.ts` 改写，**把 `criminal` 和 `ai-law` 的 TabType 联合类型、subTabs、SUB_TAB_META、组件 import、isFullBleed、渲染条件全部删掉** → 线上从 b0dacc3 构建的 bundle `index-BKrIbeKN.js` 无 刑事辩护/AI+法律（但 public/criminal/plan.html 静态文件仍更新，导致"plan 页有内容、tab 导航没了"的割裂观感）。非缓存，是配置被冲。
- **修复步骤**：
  1. `git fetch` → 远端已是 `b0dacc3`（我的本地 origin/main 缓存滞后，push 先被 non-fast-forward 拒绝，需先拉）。
  2. `git rebase origin/main`：`8a40185`(纯 plan.html) 干净接到 b0dacc3 之上，无冲突。
  3. 手动把 `criminal` + `ai-law` 重新加回三处（保留 b0dacc3 的保险 tab）：types.ts TabType 联合、`navConfig.ts` subTabs/SUB_TAB_META、`App.tsx` import/isFullBleed/渲染条件。
  4. `npx tsc --noEmit` 零错误（首次 import 编辑未生效导致 TS2304，重做后通过）。
  5. commit + push → `b0dacc3..143096d`，CI 重建 bundle `index-BT487HMs.js`，线上验证**同时含 刑事辩护/AI+法律/保险·法律维权**；SPA plan.html 也上线（id="app"+var DAYS）。
- **经验教训（高优先级）**：本仓库**多会话并发改同一组核心文件（App.tsx/navConfig.ts/types.ts）极易互相覆盖**。今后加 tab 前先 `git fetch`+确认 origin/main 最新，改完立刻 push；若 push 被拒先 rebase 再看 diff，别假设是缓存。新增 tab 时务必同时检查这三个文件+组件 import 四处，少一处整 tab 不渲染。
- 提醒用户：浏览器可能缓存旧 bundle，**硬刷新（Cmd/Ctrl+Shift+R）** 才能看到新 tab。

## 30 天计划：Day16–22「跑出来的内容」嵌入（续 Day01–15）
- 用户指令："继续跑16-22天的"。将 Day16–22 的 `.rest` 占位替换为真实产出 `.out` 块（沿用 Day14 `.out` 格式：out-h 标题 + .doc/.box/table/.meta自检）。
- 各日交付物：D16 定性之争攻防要点对照表(职务侵占/劳动借贷/挪用资金 三方)＋薄弱环节点评；D17 量刑三步法计算表＋法官四质疑回应；D18 类案检索方案(数据库/检索式/筛选/对比表模板/五误区)；D19 对财务王某调查笔录＋取证决策(自取vs申请调取)；D20 谅解书条款建议(避"以钱买刑"表述)＋谈判复盘；D21 庭前会议六项申请清单＋发言措辞清单；D22 三套发问提纲(被告/张某/鉴定人)＋"记不清"弹劾预案。
- 技术：Python 解析 `var DAYS` JSON → 正则 `re.sub(r'<div class="rest">.*?</div>', out, ...)` 替换 → `json.dumps(arr, ensure_ascii=False)` 回写；同时置 has_out=True。Node 桩渲染校验 7 天均含 .out/面包屑/下一篇；div 标签全配对。
- 已就绪天数 15→22；Day23–30 仍 `.rest` 待续。改动仅 public/criminal/plan.html，无需构建，commit+push 即上线。

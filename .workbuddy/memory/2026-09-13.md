# 2026-09-13

## 手机端站点未更新排查
- 用户反馈手机浏览器打开 linqiongni.top 没看到导航重构（个人/法务实务 两级）。
- 排查：curl 线上 `index.html` → 引用 `assets/index-tHuGm_xb.js`；拉该 JS 确认含「法务实务」「个人」→ **部署已成功**。线上无 Service Worker / manifest（非 PWA 顽固缓存）。
- 结论：用户手机本地 HTTP 缓存旧 index.html/JS，非部署问题。
- 给用户方案：手机开无痕模式验证 → 清站点数据（iOS Safari 设置→Safari→清除历史与网站数据；Android Chrome 站点设置清除存储）。
- 待确认：是否新增 `public/_headers` 让 `/index.html` 设 `Cache-Control: no-cache`（JS/CSS 带 hash 仍长缓存），使发布后手机更快刷新。用户未表态。
